#js-parallax-window.parallax-window
  .parallax-static-content
    .tabletka
      b Вы можете заказать консультацию, отправив online заявку
      input#order.order(type='button', value='Оставить заявку', onClick='showMod()')
  #js-parallax-background.parallax-background

.modorder#modorder(onClick='hideMod()')
form#form1(action='/components/order/send.php', method='POST')
  p Оставьте заявку
  input(type='text',  required="", name="name1", placeholder='Имя')
  input(type='text',  required="", name="phone1", placeholder='Телефон')
  textarea(name='mess', placeholder='Сообщение')
  input(type='submit', value='Отправить')
